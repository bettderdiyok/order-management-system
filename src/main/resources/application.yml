spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oms_database
    username: betty2201
    password: betty2201

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true